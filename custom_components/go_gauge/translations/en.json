{
  "config": {
    "step": {
      "user": {
        "title": "Add Go Gauge HA workspace",
        "description": "One workspace per integration instance: pick a friendly name and paste that workspace's Go API token. More workspaces = just add the integration again. Tokens are validated live on save; if the network is down you can skip validation.",
        "data": {
          "workspace_name": "Workspace name (e.g. Honcho, App, Default)",
          "token": "This workspace's Go API token",
          "skip_validation": "Skip validation (save offline)"
        }
      }
    },
    "error": {
      "cannot_connect": "opencode.ai is unreachable right now — skip validation or try again later.",
      "invalid_token": "Invalid token (HTTP 401) — please check or enable 'Skip validation'.",
      "missing_fields": "Please provide both name and token."
    },
    "abort": {
      "already_configured": "This token is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Go Gauge HA options",
        "data": {
          "workspace_name": "Workspace name",
          "warn_percent": "Warning threshold (%)",
          "auto_update_usage": "Auto-update usage",
          "usage_refresh_minutes": "Usage refresh interval (minutes)",
          "auto_update_models": "Auto-update models",
          "models_refresh_minutes": "Models refresh interval (minutes)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "usage": { "name": "{window} Usage" },
      "reset": { "name": "{window} Reset" },
      "forecast": { "name": "{window} Forecast" },
      "pace": { "name": "{window} Pace" },
      "remaining": { "name": "{window} Remaining" },
      "time_to_reset": { "name": "{window} Time to Reset" },
      "burn_rate": { "name": "{window} Burn-Rate" },
      "model_catalog": { "name": "Models" },
      "live_models_count": { "name": "Live Models" },
      "cheapest_model": { "name": "Cheapest Model" },
      "free_models": { "name": "Free Models" }
    },
    "binary_sensor": {
      "rate_limited": { "name": "{window} rate-limited" },
      "subscription_active": { "name": "Subscription Active" },
      "api_reachable": { "name": "API Reachable" }
    },
    "number": {
      "warning_threshold": { "name": "Warning Threshold" },
      "pace_red_limit": { "name": "Pace Red Limit" },
      "usage_refresh_min": { "name": "Usage Refresh (min)" },
      "models_refresh_min": { "name": "Models Refresh (min)" }
    },
    "switch": {
      "auto_update_usage": { "name": "Auto Update Usage" },
      "auto_update_models": { "name": "Auto Update Models" }
    },
    "button": {
      "refresh": { "name": "Refresh" }
    }
  }
}
