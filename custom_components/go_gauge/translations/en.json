{
  "config": {
    "step": {
      "user": {
        "title": "Set up Go Gauge HA",
        "description": "OpenCode Go API tokens - one per line, one per workspace. The integration queries opencode.ai directly (no monitor/dashboard needed).",
        "data": {
          "tokens": "API tokens (one per line)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach opencode.ai.",
      "invalid_token": "Invalid token (HTTP 401).",
      "no_tokens": "At least one token is required."
    },
    "abort": {
      "already_configured": "This token combination is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Go Gauge HA options",
        "data": {
          "warn_percent": "Warning threshold (%)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}